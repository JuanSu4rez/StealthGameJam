using UnityEngine;
using System.Collections;

public enum SoldierStates
{
    _none,
    patrolling,
    attacking,
    searching,
    returningtopatroll
}

public enum SoldierAnimationStates
{
    _none,
    idle,
    firing,
    searching,
    running,
    walking,
}
